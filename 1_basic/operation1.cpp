#include <iostream>

int main() {
    int x = 10;
    std::cout << "The value of x is: " << x << std::endl;
    //procceds from left to right direction, and the whenever encountered by the <<, then the right is inserted with the left side one
    return 0;
}